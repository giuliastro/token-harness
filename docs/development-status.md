# Development status

- 2026-09-16: branch-only frugal OAuth smoke confirmed Claude Code `2.1.269` subscription OAuth, Haiku, GitNexus `1.6.12` MCP connectivity and real `mcp__gitnexus__context`/`query` tool use without paid overage. The smoke is not selection evidence and no full campaign was launched. Follow-up hardening makes the real campaign manual-dispatch-only, defaults to Haiku + one attempt per variant, applies `--max-budget-usd` only to API-key runs, and fails closed if an optimized pair lacks an actual GitNexus tool-use witness.
Current focus: rerun the autonomous GitNexus selection campaign on the exact reviewed Claude/Linux tuple after the evaluation runner is on a GitNexus-supported Node runtime and a real Claude credential is available.

- Current `main`: `1526976b91f9a84f05cf39885f5469012c2dd57e` after PR #299. Final PR CI run `35059397663` and post-merge `main` CI run `35059542858` were fully green on Ubuntu, macOS and Windows, including the GitNexus collector self-test on every platform, Windows RTK live, build, bundle/package and install smoke.
- PR #299 merged the autonomous evaluation-only GitNexus campaign for exact Claude Code `2.1.269` × GitNexus `1.6.12` × native Linux. It drives the normal candidate lifecycle and benchmark evidence pipeline around 8 paired read-only Claude headless tasks, keeps MCP availability separate from observed `mcp__gitnexus__*` tool use, and leaves ordinary PR CI model-free through a cross-platform synthetic self-test.
- First trusted-`main` campaign run `35059542860` failed closed before any Claude model call because neither repository secret `ANTHROPIC_API_KEY` nor `CLAUDE_CODE_OAUTH_TOKEN` exists. No real selection evidence or Claude usage was produced by that run.
- The same run exposed an evaluation-runner compatibility issue before authentication: GitNexus `1.6.12` declares Node `^22.18.0 || >=24.11.0`, while the first workflow used the Token Harness CI floor `22.13.0`. Branch `codex/gitnexus-eval-node-runtime` moves **only the disposable real-campaign workflow** to exact Node `22.18.0`; ordinary Token Harness CI/runtime remains on Node `22.13.0`.
- PR #299 and the Node follow-up remain explicitly **GitNexus-selection-only evidence**. The autonomous runner does not install or activate RTK or HarnessTrim. Current reviewed RTK/HarnessTrim Claude compatibility rows do not include Claude Code `2.1.269`, so the machine-readable report records `combinedProductionStackEvidence.state = "not-collected"`; no combined-stack compatibility claim may be inferred from this campaign.
- The autonomous campaign may perform `gitnexus analyze --index-only` only inside the disposable evaluation runner and records that one-time indexing cost separately. Shipped Token Harness runtime/lifecycle behavior remains unchanged: no GitNexus installation, setup, indexing, hooks or skills automation.
- Latest runtime-changing `main`: `3d7f8476b9482b9ea7bbfd03d6cdaac216db4861` after PR #296. PR #297 (`b037b06ae69e45100353b420856c6ea8fc57ab05`) is documentation-only and records this handoff; its PR CI run `35012471427` and post-merge `main` CI run `35012690122` were green on Ubuntu, macOS and Windows, including the Windows RTK live smoke, build, bundle/package and install smoke. Future documentation-only handoffs do not invalidate this runtime checkpoint.
- PR #296 closed the distinction between GitNexus MCP availability and real task usage, added the reproducible real-campaign runbook, and aligned the README to the only reviewed Claude campaign row. Final PR CI run `35011878843` and post-merge `main` CI run `35012170874` were green on Ubuntu, macOS and Windows, including the Windows RTK live smoke, build, bundle/package and install smoke.
- Current release: `0.1.11`; release PR #275 merged after full Ubuntu, macOS and Windows CI.
- Managed production stack remains RTK + HarnessTrim.
- mcptoon, GitNexus and Headroom remain candidates; none is promoted by this checkpoint.
- 2026-09-15: PR #295 merged the GitNexus campaign-only index-readiness gate. New GitNexus campaign starts require exact reviewed GitNexus `1.6.12` and a machine-readable `gitnexus status --json` result with schema 1 and `status: "up-to-date"`; stale, missing, malformed or unavailable index state fails closed with manual `gitnexus analyze --index-only` remediation. The gate is intentionally absent from `apply/uninstall --candidate gitnexus`, so the reviewed MCP lifecycle remains usable without an index. Token Harness does not install GitNexus or run analyze/setup/indexing/hooks/skills. Final PR CI run `34940852899` and post-merge `main` CI run `35010585661` were green on Ubuntu, macOS and Windows, including the Windows RTK live smoke, build, bundle/package and install smoke.
- 2026-09-15: GitNexus MCP `usable` at benchmark start and finish proves runtime availability, not that Claude called a GitNexus tool. `main` therefore does not map GitNexus boundary availability into the promotion `activation-verification` gate. That gate remains blocked until a separately reviewed real-usage witness exists. mcptoon keeps its existing usage-witness semantics unchanged.
- Real-campaign execution is now at the external authentication boundary. After the evaluation-only Node fix is validated and merged, a trusted `main` run still requires one real repository Actions secret (`ANTHROPIC_API_KEY` or `CLAUDE_CODE_OAUTH_TOKEN`). Do not fabricate usage evidence or replace this with an unauthenticated CI surrogate.
- 2026-09-14: PR #282 fixed the redundant candidate-lifecycle `NEXT STEP token-harness verify` through the generic optional `requestedStateVerified` report bit. PR CI and post-merge `main` CI were green on Ubuntu, macOS and Windows; ordinary managed apply still recommends `verify` when that separate integration check remains useful.
- mcptoon evidence is pinned to the exact reviewed 0.7.10 build and includes benchmark capability, managed lifecycle, passive activation, privacy-bounded cached-schema manifest footprint evidence, project maturity, and exact Linux compatibility rows for the reviewed Codex and Claude Code versions.
- The manifest-footprint reader is fail-closed on receipt shape, exact version, aggregate arithmetic and timestamp ordering; the campaign exposes only the latest cached-schema snapshot rather than cumulative savings.
- mcptoon selection campaigns now fail closed outside the exact reviewed native-Linux rows: Codex 0.152.1, Codex 0.153.0, or Claude Code 2.1.269. The Codex 0.153.0 point has its own real Ubuntu compatibility/reversibility recording; it is not a widened semver range. Optimized starts additionally require mcptoon 0.7.10; Windows, macOS, WSL, different harness versions and other harness families cannot create selection evidence.
- 2026-09-14: real Codex campaign `mcptoon-real-codex` completed all 8/8 pairs with quota-backed evidence, 16/16 quality passes, one attempt per task, and no inconclusive/incomparable pair. Selection was mixed: 4 equivalent pairs, 2 optimized wins and 2 baseline wins. The campaign is decision-ready but not promising.
- Quota evidence shows no consistent saving signal. Local token usage stayed unavailable, so no local-token claim is made.
- Context evidence shows no demonstrated reduction: 7 pairs were stable and 1 critical pair is unknown because the optimized capture changed from 155 to 165 static MCP tools during the task. The following critical baseline started at 165 and stayed there, so the persisted surface drift is not attributable to mcptoon from the available receipts. The exact source of the ten additional tools is not recoverable from the privacy-bounded snapshots.
- Timing evidence is observational only: optimized wall clock was about 5.6% slower cumulatively in this campaign. This is not a statistical conclusion.
- Compatibility/reversibility evidence remains positive only for the exact reviewed tuple/lifecycle. Separately, the campaign activation witness recorded no successful mcptoon activity inside the optimized task windows, so the observed quota/timing differences cannot be attributed to candidate execution.
- Decision: **KEEP EXPERIMENTAL**. mcptoon stays out of `PROVIDER_ADAPTERS` and out of the RTK + HarnessTrim production stack. Do not claim token, quota, context or timing savings from this campaign.
- 2026-09-14 candidate choice: evaluate **GitNexus before Headroom**. GitNexus provides a distinct repository-exploration/code-intelligence mechanism that can complement RTK/HarnessTrim, while Headroom's broad proxy/context rewriting overlaps more directly with existing optimization ownership and would be harder to attribute and reverse cleanly.
- GitNexus remains pinned for managed evidence to `1.6.12`. Upstream is active: stable `1.6.11` shipped 2026-09-04, stable `1.6.12` shipped 2026-09-12, and `1.6.13-rc.1` followed on 2026-09-14. The release candidate does not widen the reviewed stable tuple.
- 2026-09-14: PR #284 merged the GitNexus exact-version hardening. Candidate observation fails closed for any benchmark version other than exact `1.6.12`; an unreviewed version such as `1.6.13-rc.1` stops after the passive version probe and cannot become `benchmark-ready` merely by advertising compatible CLI surfaces. PR CI and post-merge `main` CI were green on Ubuntu, macOS and Windows.
- 2026-09-14: PR #285 aligned public `context` and promotion-readiness diagnostics with that exact-version semantics. An unreviewed GitNexus build is shown as `unsupported-version`, never as benchmark-ready or merely "below" a generic minimum, and the reviewed `1.6.12` pin is exposed explicitly. Behavioral coverage asserts that the unsupported-version path stops after `gitnexus --version`. PR CI and post-merge `main` CI were green on Ubuntu, macOS and Windows, including the Windows RTK live smoke.
- 2026-09-14: PR #286 merged GitNexus benchmark-version provenance. New candidate sidecars persist the passively observed exact `1.6.12` build; legacy or mismatched GitNexus sidecars remain parseable historical data but fail closed for candidate-specific matrix/campaign evidence and are never retrofitted by rewriting benchmark receipts. Matrix/report reads remain passive and work without a currently installed GitNexus binary. PR CI and post-merge `main` CI were green on Ubuntu, macOS and Windows, including the Windows RTK live smoke.
- 2026-09-14: PR #287 merged GitNexus baseline-purity hardening. Candidate attribution requires the native MCP witness to report `absent` at baseline start and, once a baseline receipt exists, at both start and finish. `usable`, `unusable`, `unknown`, missing legacy witness data, malformed baseline artifacts, or a missing baseline fail closed for GitNexus candidate evidence while ordinary benchmark receipts remain readable. The existing benchmark witness is reused, so no extra MCP scan was added. PR CI and post-merge `main` CI were green on Ubuntu, macOS and Windows, including the Windows RTK live smoke.
- 2026-09-14: PR #288 merged RFC 0009 campaign admission for GitNexus. New starts require an exact row for GitNexus `1.6.12`, observed harness version, OS and WSL state before the GitNexus binary is probed. The PR and post-merge `main` CI were green on Ubuntu, macOS and Windows, including the Windows RTK live smoke.
- 2026-09-14: PR #289 merged the first real GitNexus RFC 0009 compatibility row from successful GitHub Actions recording `34878184649`: exact Claude Code `2.1.269` × GitNexus `1.6.12` × Linux x64 non-WSL, verification tier `config-only`. The isolated recording used production `merge-json` ownership for `mcpServers.gitnexus`, produced a real `owned-json-entry`, refused surgical removal after user drift, verified rollback to the original brownfield state, and verified clean uninstall while preserving both pre-existing and post-apply user MCP/settings changes. No `gitnexus setup`, analyze, indexing, hooks, skills, MCP server execution or Codex configuration occurred. Adjacent versions/platforms remain refused. PR CI and post-merge `main` CI run `34889007011` were green on Ubuntu, macOS and Windows, including the Windows RTK live smoke.
- 2026-09-15: PR #291 merged the narrow GitNexus candidate lifecycle into the normal CLI without promoting GitNexus. `apply --candidate gitnexus --harness claude` is admitted only on the exact recorded Claude Code `2.1.269` × GitNexus `1.6.12` × native-Linux non-WSL row and only registers the already-installed CLI as the owned Claude MCP entry; it never installs GitNexus or runs setup/analyze/indexing. `uninstall --candidate gitnexus --harness claude` is ownership-driven, needs no GitNexus binary, refuses user-owned/drifted entries, and treats a later committed removal as invalidating older activation receipts. The existing mcptoon lifecycle implementation is preserved byte-for-byte behind a candidate dispatcher. Final PR CI run `34924201588` and post-merge `main` CI run `34924357457` were green on Ubuntu, macOS and Windows, including the Windows RTK live smoke.
- 2026-09-15: PR #293 merged the GitNexus campaign planner wiring for that reviewed Claude lifecycle only. The planner emits owned deactivate → baseline start, owned activate → optimized start, and optimized finish → owned cleanup commands for GitNexus + Claude; it still does not execute benchmark workloads automatically, install/index GitNexus, widen compatibility, configure Codex, or promote the candidate. Existing GitNexus + Codex behavior remains the external/manual activation path, and existing mcptoon orchestration is unchanged. Final PR CI run `34925515624` and post-merge `main` CI run `34938924456` were green on Ubuntu, macOS and Windows, including the Windows RTK live smoke.
- Existing PR #265 provides the minimum transactional GitNexus candidate primitive for the exact 1.6.12 CLI on Claude Code user MCP JSON configuration. It does not install GitNexus, run setup/analyze/indexing, touch hooks/skills, support Codex TOML ownership, or add GitNexus to `PROVIDER_ADAPTERS`.
- GitNexus licensing is an explicit blocker for generic managed production promotion: the exact 1.6.12 package and upstream LICENSE declare `PolyForm-Noncommercial-1.0.0`. The reviewed lifecycle can remain candidate evidence, but Token Harness must not infer commercial-production permission or auto-install/promote the candidate from technical maturity alone.
- Current GitNexus gate direction: managed lifecycle is passed for the narrow Claude 1.6.12 MCP configuration primitive and one exact Claude 2.1.269 × Linux non-WSL RFC 0009 point is recorded. Project/license maturity remains blocked for generic production promotion until suitable license terms or an appropriate license review admit the intended deployment. Codex ownership, compatibility breadth, combined-stack validation, a real usage witness and real selection evidence remain independent gates.
- Real-campaign runbook: `docs/candidates/gitnexus-real-campaign.md`. It requires an unchanged repository commit, an explicit `gitnexus status --json` readiness check, manual `gitnexus analyze --index-only` only when needed outside the disposable autonomous runner, strict baseline MCP absence, optimized MCP availability, equivalent paired conditions and no savings claims beyond measured evidence.

- 2026-09-13: mcptoon candidate campaigns now have a Token Harness-managed transactional lifecycle on exact reviewed rows. `apply --candidate mcptoon` can install the pinned 0.7.10 build through existing pipx and activate owned guidance; `uninstall --candidate mcptoon` surgically deactivates only owned guidance between paired runs. mcptoon remains candidate-only and is not in `PROVIDER_ADAPTERS`.