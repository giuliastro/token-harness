name: One-shot guided uninstall undo

on:
  push:
    branches:
      - feat/guided-uninstall-undo

permissions:
  contents: write

jobs:
  patch-test-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/guided-uninstall-undo
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version: '22.13.0'
          cache: pnpm
      - name: Apply guided uninstall undo patch
        run: python3 scripts/one-shot-guided-uninstall-undo.py
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Format
        run: pnpm format
      - name: Typecheck
        run: pnpm typecheck
      - name: Lint
        run: pnpm lint
      - name: Test
        run: pnpm test
      - name: Commit validated product changes
        shell: bash
        run: |
          rm .github/workflows/one-shot-guided-uninstall-undo.yml scripts/one-shot-guided-uninstall-undo.py
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "feat(ui): undo guided uninstall by transaction"
          git push origin HEAD:feat/guided-uninstall-undo
